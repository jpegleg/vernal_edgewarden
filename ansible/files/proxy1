#!/usr/bin/env sh
CMDZ="$1"
PROCNM="kiaproxy1"

skillo() {
  pkill $PROCNM && pgrep $PROCNM && pkill -9 $PROCNM
}

if [ "$CMDZ" == "start" ]; then  
  pgrep $PROCNM || export SERVERS=127.0.0.1:3443 && cd /root && /root/kiaproxy1 > /var/log/kiaproxy1.log 2>&1 &
fi
if [ "$CMDZ" == "restart" ]; then
  skillo && export SERVERS=127.0.0.1:3443 && cd /root && /root/kiaproxy1 > /var/log/kiaproxy1.log 2>&1 &
fi
if [ "$CMDZ" == "stop" ]; then
  skillo
fi
